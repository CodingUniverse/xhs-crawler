# Next.js Production Environment Variables
# Copy this to .env.production and adjust values as needed

# API URL - use empty string for relative path (works with Nginx proxy)
# The frontend will fetch from /api/* which Nginx will proxy to backend
NEXT_PUBLIC_API_URL=

# Base path for API (used by Nginx proxy)
NEXT_PUBLIC_API_BASE=/api